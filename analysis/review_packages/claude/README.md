# Claude review package

Current manuscript-review archive:
`CLAUDE_SCIENCE_CURRENT_INPUT_2026-07-31.zip`. Start with
`CLAUDE_SCIENCE_HANDOFF_CURRENT.md` (included in the archive as
`START_HERE.md`). The resulting audit and proposed revisions are retained as
`MANUSCRIPT_AUDIT_2026-07-31.md`, `REVISED_SECTIONS_EN_2026-07-31.md`, and
`DECISIONS_ZH_2026-07-31.md`.

The accepted wording changes were merged selectively into
`analysis/MANUSCRIPT_DRAFT_STREAMLINED.md`. One audit statement was narrowed:
the evidence supports that all fully frozen **independent external** route
validations are currently null, rejected, or abstaining; it does not support
the broader statement that every outcome-frozen analysis is negative, because
the within-campaign KIT analysis used an internal freeze and was positive.

Older archives below are retained as provenance and are not current manuscript
inputs.

Start with `analysis/ADVERSARIAL_REVIEW_RESPONSE.md`, then inspect
`ADVERSARIAL_REFEREE_REPORT_ORIGINAL.md`, `PROMPT_FOR_CLAUDE.md`, and the revised
paper files in the listed order. The bundle contains only
manuscript documents, compact derived results, figures, frozen designs, and
verification records. It deliberately excludes the SQLite database, raw source
rows, large trajectory tables, credentials, and any source whose redistribution
rights are unresolved.

The positive scientific thesis is fixed: neighboring experimental domains can
provide useful and complementary information when sources are qualified,
matched to the endpoint, preserved as independent proposals, and protected by
wrong-source controls. KIT predictive transfer, Caltech external source
rankings, and the diagnostic neighbor portfolio provide component-level proof
of feasibility. The added CCA family-first audit tests whether that portfolio
recovers distinct high-value regions rather than repeated within-component
entities, includes the failed multiplicative local gate, and supplies an
outcome-unseen freeze protocol. That protocol has now been executed on
Starrydata and TRI: neither target passes its complete gate, and the pooled
effect is null and heterogeneous. The artifact-gated map therefore retains
positive, null, harmful, and unresolved regions and demonstrates abstention
rather than a general transfer law. The integrated strategy is not a
prospective discovery-acceleration result.

The v5 addition is the independently reconstructed leave-one-program gate
benchmark across 97 edges, 20 tasks, and 13 programme clusters. Its positive
point utility and low clear-harm count coexist with failed Holm superiority and
inferior clear-benefit retention relative to adjacency-only. Claude should
judge whether this sharpens the contribution into a defensible result:
adjacency is a useful first-order proposal prior, but reliable borrowing needs
candidate-local, endpoint-specific applicability. The CCA-v2 architecture is
included as a prospective commitment, not completed evidence.
