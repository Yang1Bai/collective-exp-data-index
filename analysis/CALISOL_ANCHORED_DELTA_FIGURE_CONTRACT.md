# Figure 2 panel-c contract: CALiSol provenance-anchored contrast transfer

**Core conclusion:** On the same held-article-out CALiSol edge where absolute
donor-feature transfer was unresolved, changing the transferred object to a
within-article response relation and supplying one target-article anchor
reduced prediction error in 8 of 11 articles and passed the frozen
method-development gate.

**Figure archetype:** quantitative grid; panel c is the paired article-level
hero validation for the method change.

**Target/output:** *Digital Discovery*, double-column Figure 2, Python-only
matplotlib exports in editable SVG/PDF plus 600-dpi TIFF and PNG preview.

**Panel map:**

- **a:** strong pooled alloy relation and failed coefficient transport;
- **b:** KIT positive absolute feature transfer and original CALiSol absolute
  boundary;
- **c:** matched CALiSol absolute-versus-contrast RMSE for every independent
  held-out article;
- **d:** pooled adsorption regularity and artifact/condition gates.

**Evidence hierarchy:** Panel c is the direct evidence that the transferred
object matters. The diagonal is the matched null. Points below it are articles
where contrast transfer improves on the absolute donor; harmful articles use a
different marker. Text reports the article-macro effect, article bootstrap
interval, exact sign-flip test and shuffled falsifier. Random-anchor robustness
is reported in the caption and source data.

**Statistics:** \(n=11\) independent source articles. Each point is one
article-level RMSE over non-anchor formulations. The portfolio effect is the
unweighted mean article RMSE ratio. The interval is a 10,000-replicate
article-cluster bootstrap; \(p=0.0352\) is an exact one-sided sign-flip test.
The shuffled-source \(p=0.005\) uses 199 within-article donor-label
permutations.

**Source data:** formal E6 prediction, article-metric, summary, shuffled-null
and verified-result files. Panel source data must contain all 11 articles;
none may be filtered for visual convenience.

**Reviewer risks:** E6 is post-outcome method development; this must be stated
in the caption. Anchor rows must not enter test RMSE. The three harmful
articles must remain visible. The panel cannot imply zero-shot, universal, or
independently replicated transfer.

