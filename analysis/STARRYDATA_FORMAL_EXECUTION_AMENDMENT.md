# Starrydata formal-execution amendment after non-claim smoke access

**Recorded:** 2026-07-18, after the hash-frozen two-repeat smoke run first
parsed target ZT and before the 100-repeat formal run.

The pre-outcome design, schema, implementation JSON, target metadata, source
ranks, policy orders, and hypothesis cards remain unchanged. The smoke run was
used only to test target joining, task completion, and row-count arithmetic. It
is permanently labelled `smoke-nonclaim` and cannot enter a manuscript result.

The code audit identified two execution items already required by the frozen
implementation but not yet materialized as standalone formal artifacts:

1. deterministic group-preserving source-size controls and target-coverage
   matched scopes for every ionic-source/wrong-source pair; and
2. a formal verifier/summarizer that uses the saved independent component and
   provenance error rows for the predeclared two-level uncertainty analysis.

These items are implemented without changing the target, extraction, sources,
primary cell, learners, representations, label budgets, endpoint, policies,
hypothesis cards, or multiplicity families. The control generator is forbidden
from reading the curve `y` column. The formal scripts must verify the original
PREOUTCOME sentinel and must report every already named method and control.
Because these executable scripts were completed after target-outcome access,
their timing is disclosed and their results cannot be used to invent or replace
a primary contrast. Any further scientific-method change requires a new target.

